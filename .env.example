# Local VOXL monorepo environment. Copy with: bun run env:init
APP_ENV=local
NODE_ENV=development

STUDIO_URL=http://127.0.0.1:5740
SERVER_URL=http://127.0.0.1:5741
DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:56422/postgres

# Added when the corresponding product phases begin.
OBJECT_STORAGE_ENDPOINT=
OBJECT_STORAGE_BUCKET=
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# Optional logging controls for local management scripts.
# LOG_COLOR=auto|always|never
# LOG_UNICODE=auto|always|never
