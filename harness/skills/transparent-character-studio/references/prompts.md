# Translation rules

Convert concrete creative requests into the smallest number of readable frames. Preserve a distinct palette key for each requested color. For blinking, smoke, sparks, and similar regular effects, use deterministic animation settings rather than inventing a new rendering system. Ask only when the user’s requested asset size or duration materially changes the deliverable.
